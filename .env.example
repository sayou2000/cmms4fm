ENABLE_EMAIL_NOTIFICATIONS=false
INVITATION_VIA_EMAIL=false
MAIL_TYPE=smtp
SMTP_PWD=
SMTP_USER=
SMTP_FROM=
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
POSTGRES_USER=rootUser
POSTGRES_PWD=mypassword
JWT_SECRET_KEY=sD1HBM6ngcaDLMzDqgA9Pn9LEECNAp0C1EOHIR/D+q4=
ALLOWED_ORGANIZATION_ADMINS=
GOOGLE_KEY=
GOOGLE_TRACKING_ID=
PUBLIC_SERVER_URL=http://localhost:3000
GCP_BUCKET_NAME=
GCP_JSON=
GCP_PROJECT_ID=
MINIO_USER=minio
MINIO_PASSWORD=minio123
MAIL_RECIPIENTS=
STORAGE_TYPE=MINIO
LICENSE_KEY=
ENABLE_SSO=false
OAUTH2_PROVIDER=
OAUTH2_CLIENT_ID=
OAUTH2_CLIENT_SECRET=
LOGO_PATHS=
CUSTOM_COLORS=
BRAND_CONFIG=
DEMO_LINK=
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=
LDAP_ENABLED=false

# Fork-specific switches. Also add them to the api service in docker-compose.yml when adding
# more, or they never reach the container - that block enumerates its environment explicitly.
SELF_HOSTED_UNLOCK_PREMIUM=false
AUTOMATION_ENABLED=false
# Changed rows one transaction may announce to the rule engine before the rest is dropped.
AUTOMATION_MAX_CHANGES_PER_TRANSACTION=200
TRIAGE_ENABLED=true

# MCP server (docs/mcp-server-konzept.md). Same rule as above: the mcp service in
# docker-compose.yml enumerates its environment, so a variable added here has to be
# referenced there too. Requires SELF_HOSTED_UNLOCK_PREMIUM=true to be of any use, because
# the whole x-api-key path is gated behind it.
# PROFILE: readonly | core | core-readonly | assets | workorders | full
MCP_PROFILE=readonly
MCP_READ_ONLY=false
# passthrough: each client presents its own API key. service: the server holds one key.
MCP_AUTH_MODE=passthrough
MCP_SERVICE_API_KEY=
MCP_TOOLS_ALLOW=
MCP_TOOLS_DENY=
MCP_RATE_LIMIT=120
MCP_SPEC_REFRESH_MINUTES=0
MCP_MAX_RESPONSE_CHARS=60000
MCP_LOG_LEVEL=info
